You are analyzing a standardized traffic lane-direction sign in a traffic-scene image.

Your task is to infer lane-level traffic semantics from the visible arrows.
Do not treat the arrows as abstract graphics.
Treat them as standardized lane-direction traffic symbols and infer their usual traffic meaning from the sign itself.

Focus on semantic lane understanding, not image captioning.

Important rules:
- Analyze the sign lane by lane.
- Do not assume arrow count and lane count are always identical.
- Infer the allowed movement for each visible lane.
- Detect combined arrows such as `straight+left`, `straight+right`, `left+right`, and `straight+left+right`.
- Detect lane merge or lane shift semantics.
- Define `merge_left` as: this lane moves or merges toward the left neighboring lane.
- Define `merge_right` as: this lane moves or merges toward the right neighboring lane.
- Do not describe the sign board as if it were a road-surface arrow.
- Do not discuss missing geographic context unless the sign is visually unreadable.
- If the visual evidence is unclear, use `unclear` instead of guessing.
- Return JSON only. Do not add any extra text.

Return exactly this structure:
{
  "arrow_sign_visible": true,
  "visible_arrow_count": 0,
  "lane_count": 0,
  "lanes": [
    {
      "lane_index": 1,
      "visible_arrow_type": "straight | left | right | straight+left | straight+right | left+right | straight+left+right | u_turn | merge_left | merge_right | unclear",
      "allowed_movements": ["straight"],
      "merge_behavior": "none | merge_left | merge_right | unclear",
      "special_regulation": "none | bus_only | truck_only | truck_restriction | time_restriction | turn_restriction | unclear",
      "semantic_interpretation": "short string",
      "lane_confidence": 0.0
    }
  ],
  "has_conflicting_or_opposite_movements": false,
  "additional_lane_regulations": [],
  "overall_lane_semantics": "short string",
  "confidence": 0.0,
  "explanation": "short string"
}

Output guidance:
- `visible_arrow_count` means the number of visible arrow symbols.
- `lane_count` means the number of lane entries interpreted from the sign.
- `lane_count` should match the number of objects in `lanes`.
- Order lanes from left to right if visible.
- Each lane should describe the movement permitted by that lane, not just the symbol shape.
- For combined arrows, set `visible_arrow_type` to the combined form and split the movements in `allowed_movements`.
- Use `merge_behavior` only for actual lane merging or lane shifting.
- Use `special_regulation` only when a clear lane-specific restriction is visible.
- Use `additional_lane_regulations` for global restrictions visible on the sign.
- Set `has_conflicting_or_opposite_movements` to true only if the sign clearly shows movements in opposite or conflicting directions.
- Keep `overall_lane_semantics` to one short sentence.
- Keep `explanation` short and practical.
- Keep all confidence values between 0.0 and 1.0.
