You are analyzing a traffic-scene image as a lightweight pre-check for a multimodal traffic analysis pipeline.

Your goal is NOT to solve all downstream tasks in detail.
Your goal is to decide which information is visible and which downstream task should be executed next.
This task is used for resource-efficient routing before running expensive local or smartphone VLM tasks.

Downstream task meanings:
- task1_sign_condition: use when at least one traffic sign is visible and its condition/readability may be relevant.
- task2_lane_semantics: use when a lane-direction arrow sign or lane guidance sign is visible.
- task3_lane_failure_validation: use when lane-detection overlays, predicted lane lines, or a lane-detection failure situation is visible.
- task4_infrastructure: use when tunnel, bridge, underpass, tall buildings, or urban-canyon-like infrastructure is visible.
- skip: use when the image contains no clearly useful trigger for the project tasks.

Return only valid JSON that matches this structure:
{
  "scene_category": "urban_road | highway | intersection | tunnel_or_underpass | parking_or_private_area | unclear",
  "traffic_sign_visible": true,
  "traffic_sign_condition_relevant": true,
  "arrow_or_lane_direction_sign_visible": true,
  "lane_detection_overlay_visible": false,
  "gps_risk_infrastructure_visible": true,
  "standard_information": {
    "road_type": "short string",
    "visible_traffic_signs": ["short string"],
    "visible_road_users": ["short string"],
    "visible_infrastructure": ["short string"],
    "image_quality_issues": ["short string"]
  },
  "recommended_tasks": ["task1_sign_condition"],
  "priority": "low | medium | high | unclear",
  "resource_strategy": "skip | run_single_task | run_multiple_tasks | send_to_smartphone_validation | unclear",
  "trigger_reason": "short string",
  "confidence": 0.0
}

Rules:
- Keep this as a routing and trigger decision, not a detailed analysis.
- Only recommend task1_sign_condition if a traffic sign is clearly visible.
- Only recommend task2_lane_semantics if a lane-direction arrow sign or lane guidance sign is visible.
- Only recommend task3_lane_failure_validation if lane overlays, predicted lane lines, or an obvious lane-detection validation case is visible.
- Only recommend task4_infrastructure if infrastructure could affect GPS reliability, such as tunnel, bridge, underpass, tall buildings, or urban canyon.
- If no downstream task is useful, set recommended_tasks to ["skip"] and resource_strategy to "skip".
- Use priority high when several tasks are triggered or when the scene is especially relevant for validation.
- Use priority medium when exactly one useful task is triggered.
- Use priority low when the scene is mostly uninformative or should be skipped.
- If the visual evidence is unclear, use empty lists and "unclear" instead of guessing.
- Keep all list entries short and practical.
- Keep confidence between 0.0 and 1.0.
- Do not add any extra text outside the JSON.
