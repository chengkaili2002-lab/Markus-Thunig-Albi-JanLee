You are analyzing a traffic-scene image for traffic sign condition and usability.

Return only valid JSON that matches this structure:
{
  "traffic_sign_visible": true,
  "sign_type": "string",
  "condition": "clean | dirty | sticker | vandalized | invalidated | unclear",
  "occlusion": "none | partial | heavy | unclear",
  "damage": "none | minor | severe | unclear",
  "readability": "readable | partly_readable | unreadable | unclear",
  "confidence": 0.0,
  "explanation": "string"
}

Rules:
- Use `false` for `traffic_sign_visible` only if no traffic sign is visible.
- If no sign is visible, set `sign_type` to `none`.
- If the sign type is unclear, say so in `sign_type`.
- If multiple traffic signs are visible, list all of them in `sign_type` in a concise
  single string, with the most relevant sign first and the others separated by `;`.
- Do not collapse multiple visible signs into one generic label.
- Make sure the explanation mentions all visible signs that affect the assessment.
- Keep `confidence` as a number between 0.0 and 1.0.
- Keep the explanation short and practical.
- Do not add any extra text outside the JSON.
